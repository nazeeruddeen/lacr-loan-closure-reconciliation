apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lacr-mysql-pvc
  namespace: lacr-loan
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lacr-mysql
  namespace: lacr-loan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lacr-mysql
  template:
    metadata:
      labels:
        app: lacr-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: lacr-secret
                  key: SPRING_DATASOURCE_PASSWORD
            - name: MYSQL_DATABASE
              value: lacr_loan_closure_reconciliation
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: lacr-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: lacr-mysql
  namespace: lacr-loan
spec:
  selector:
    app: lacr-mysql
  ports:
    - port: 3306
      targetPort: 3306

