apiVersion: apps/v1
kind: Deployment
metadata:
  name: lacr-frontend
  namespace: lacr-loan
spec:
  replicas: 2
  minReadySeconds: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: lacr-frontend
  template:
    metadata:
      labels:
        app: lacr-frontend
    spec:
      containers:
        - name: frontend
          image: lacr-loan-closure-reconciliation-frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
            - name: API_UPSTREAM
              value: http://lacr-backend
          readinessProbe:
            httpGet:
              path: /
              port: 80
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /
              port: 80
            periodSeconds: 20
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: lacr-frontend
  namespace: lacr-loan
spec:
  selector:
    app: lacr-frontend
  ports:
    - port: 80
      targetPort: 80
